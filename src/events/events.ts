import clientReady from "@/events/files/clientReady";
import interactionCreate from "@/events/files/interactionCreate";

const events = {
  clientReady,
  interactionCreate,
};

export default events;
